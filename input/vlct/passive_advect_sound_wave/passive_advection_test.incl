   Boundary { type = "periodic"; }

   Stopping {
      time = 1.0;
   }

   Group {
      list = ["colour"];

      colour {
         field_list = ["red"];
      }
   }

   Field {
      # the fields required for the VL+CT integrator are automatically added
      list = ["red"];

      courant_=0.4;
      gamma = 1.6666666666666667;
      
      ghost_depth = 3;
      padding = 0;
      alignment = 8;
   }

   Output { 

      list = ["data"];

      data {
         type = "data";
         field_list = ["density", "velocity_x", "velocity_y", "velocity_z",
	      	       "total_energy", "red",
	     	       "bfield_x", "bfield_y", "bfield_z",
		       "bfieldi_x", "bfieldi_y", "bfieldi_z"];
         schedule {
            var = "time";
            list = [0.0,1.0];
         };
      };
   }