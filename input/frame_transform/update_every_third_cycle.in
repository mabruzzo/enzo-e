include "input/frame_transform/input_common.incl"

Method{

   frame_transform{
      schedule {
          var   = "cycle";
          start = 1;
          step  = 3;
      };
   };

}

Output {
    cycled { dir = [ "frame_transform-every_third_cycle_%03d", "cycle" ]; };
 }